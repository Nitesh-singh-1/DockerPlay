export interface CheatSheetEntry {
  category: 'Containers' | 'Images' | 'Networking' | 'Volumes' | 'Dockerfile' | 'Docker Compose' | 'Debugging & Logs' | 'System';
  command: string;
  syntax: string;
  description: string;
  example: string;
  explanation: string;
  commonMistakes: string;
  tip: string;
}

export const DOCKER_CHEATSHEET: CheatSheetEntry[] = [
  {
    category: 'Containers',
    command: 'docker run',
    syntax: 'docker run [OPTIONS] IMAGE [COMMAND] [ARG...]',
    description: 'Create and start a new container from an image',
    example: 'docker run -d --name web -p 8080:80 -e ENV=prod nginx:alpine',
    explanation: 'Combines `docker create` and `docker start` in a single command. Provisions a read-write container layer and boots the process.',
    commonMistakes: 'Forgetting `-d` and having the container block your active terminal, or reversing port order in `-p`.',
    tip: 'Use `--rm` for one-off CLI tools (e.g. `docker run --rm node:22 node -v`) so containers automatically clean up when done.',
  },
  {
    category: 'Containers',
    command: 'docker ps',
    syntax: 'docker ps [OPTIONS]',
    description: 'List active and running containers',
    example: 'docker ps -a',
    explanation: 'Displays running containers, their ports, status, and names. Adding `-a` reveals stopped and exited containers.',
    commonMistakes: 'Thinking a container disappeared when it simply stopped. Always check `docker ps -a` to find exited containers.',
    tip: 'Use `docker ps -q` to output only container IDs (great for scripting like `docker stop $(docker ps -q)`).',
  },
  {
    category: 'Containers',
    command: 'docker stop',
    syntax: 'docker stop [OPTIONS] CONTAINER [CONTAINER...]',
    description: 'Gracefully stop a running container',
    example: 'docker stop web-app',
    explanation: 'Sends SIGTERM signal to PID 1 inside the container, waits up to 10 seconds for graceful shutdown, then sends SIGKILL if still running.',
    commonMistakes: 'Using `docker kill` when a graceful shutdown with `docker stop` is preferred for database data safety.',
    tip: 'You can stop multiple containers at once: `docker stop web api db`.',
  },
  {
    category: 'Containers',
    command: 'docker rm',
    syntax: 'docker rm [OPTIONS] CONTAINER [CONTAINER...]',
    description: 'Remove one or more stopped containers',
    example: 'docker rm -f web-app',
    explanation: 'Deletes the container and its ephemeral read-write filesystem layer. Mounted volumes are not deleted.',
    commonMistakes: 'Trying to remove a running container without `-f` or without stopping it first.',
    tip: 'Use `docker container prune` to delete all stopped containers at once.',
  },
  {
    category: 'Images',
    command: 'docker pull',
    syntax: 'docker pull [OPTIONS] NAME[:TAG|@DIGEST]',
    description: 'Download an image from a container registry (e.g. Docker Hub)',
    example: 'docker pull postgres:16-alpine',
    explanation: 'Fetches image layer blobs and manifest from a remote registry into your local engine cache.',
    commonMistakes: 'Omitting the tag and accidentally pulling `:latest`, which may introduce unexpected breaking changes over time.',
    tip: 'Always pin explicit semantic tags (e.g. `node:22.11-alpine`) in production.',
  },
  {
    category: 'Images',
    command: 'docker build',
    syntax: 'docker build [OPTIONS] PATH | URL',
    description: 'Build an image from a Dockerfile',
    example: 'docker build -t myapp:1.0 .',
    explanation: 'Sends the build context (directory containing Dockerfile) to the Docker Daemon and executes steps to produce image layers.',
    commonMistakes: 'Forgetting the trailing dot (`.`) specifying the current directory build context.',
    tip: 'Create a `.dockerignore` file to exclude `node_modules`, `.git`, and build artifacts from the build context.',
  },
  {
    category: 'Images',
    command: 'docker images',
    syntax: 'docker images [OPTIONS] [REPOSITORY[:TAG]]',
    description: 'List local cached images',
    example: 'docker images',
    explanation: 'Displays repository names, tags, image IDs, creation timestamps, and virtual disk sizes.',
    commonMistakes: 'Confusing image size with container RAM usage. Image size is disk space.',
    tip: 'Use `docker image prune -a` to clean up unused dangling images.',
  },
  {
    category: 'Networking',
    command: 'docker network create',
    syntax: 'docker network create [OPTIONS] NETWORK',
    description: 'Create a user-defined bridge network',
    example: 'docker network create app-net',
    explanation: 'Sets up an isolated virtual bridge with embedded DNS name resolution for automatic container discovery.',
    commonMistakes: 'Relying on the default `bridge` network and wondering why containers cannot ping each other by name.',
    tip: 'Custom networks automatically enable DNS resolution between containers using their container names.',
  },
  {
    category: 'Networking',
    command: 'docker network connect',
    syntax: 'docker network connect [OPTIONS] NETWORK CONTAINER',
    description: 'Connect a container to an existing network',
    example: 'docker network connect backend-net frontend-container',
    explanation: 'Dynamically attaches an extra network interface to a running or stopped container without restarting it.',
    commonMistakes: 'Assuming a container can only belong to one network. Containers can be multi-homed to bridge distinct zones.',
    tip: 'Use multi-network setups to create secure DMZs (e.g. frontend on public & backend net, db only on backend net).',
  },
  {
    category: 'Volumes',
    command: 'docker volume create',
    syntax: 'docker volume create [OPTIONS] [VOLUME]',
    description: 'Create a named persistent volume',
    example: 'docker volume create pgdata',
    explanation: 'Allocates managed storage on the host filesystem that lives outside container lifecycles.',
    commonMistakes: 'Deleting a container and expecting volume data to be gone, or forgetting to mount the volume to a new container.',
    tip: 'Named volumes are faster and more portable across OSes than host bind mounts.',
  },
  {
    category: 'Docker Compose',
    command: 'docker compose up',
    syntax: 'docker compose up [OPTIONS] [SERVICE...]',
    description: 'Build, create, and start multi-container services',
    example: 'docker compose up -d --build',
    explanation: 'Reads `compose.yaml`, creates required networks, volumes, pulls/builds images, and starts services with dependency ordering.',
    commonMistakes: 'Forgetting `-d` and accidentally terminating the stack when closing terminal.',
    tip: 'Add `--build` if you made source code changes and need images recompiled.',
  },
  {
    category: 'Debugging & Logs',
    command: 'docker logs',
    syntax: 'docker logs [OPTIONS] CONTAINER',
    description: 'View stdout and stderr output from a container',
    example: 'docker logs -f --tail 100 web-server',
    explanation: 'Streams captured output written to PID 1 inside the container.',
    commonMistakes: 'Wondering why logs are empty if the application logs to a file inside the container instead of stdout/stderr.',
    tip: 'Always design containerized applications to log to standard output (`/dev/stdout`).',
  },
  {
    category: 'Debugging & Logs',
    command: 'docker exec',
    syntax: 'docker exec [OPTIONS] CONTAINER COMMAND [ARG...]',
    description: 'Run a command inside an active container',
    example: 'docker exec -it web-app sh',
    explanation: 'Spawns a new process inside the running container namespaces (great for interactive debugging, curl tests, inspecting env).',
    commonMistakes: 'Trying to exec into a stopped container. The container must be in `running` state.',
    tip: 'If `bash` is not found, try `sh` since minimal Alpine images only bundle `/bin/sh`.',
  },
];
